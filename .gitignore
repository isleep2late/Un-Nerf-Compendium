# Built PKHaX binary is 156 MB - exceeds GitHub's 100 MB limit. Ship it as a Release, not in git.
PKHeX.exe
PKHaX/PKHeX.exe
PKHaX/_pub/
# .NET build intermediates (in case the source is built locally)
**/bin/
**/obj/
**/__pycache__/
.vs/
*.pdb
# push_pr.sh saved remote URL
.push_pr.conf
.push_pr.command
# macOS + stray scratch files
.DS_Store
**/.DS_Store
.__wtest

# iOS/Android signing material — never commit (generate under ~/keys/, this is a backstop)
*.p12
*.key
*.csr
*.cer
*.pem
*.keystore
*.mobileprovision
*.b64
